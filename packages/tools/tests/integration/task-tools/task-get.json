{
  "name": "task.get",
  "scenarios": [
    {
      "name": "task.get with missing taskId",
      "input": {
        "tool": "task.get"
      },
      "expect": {
        "status": "error",
        "error_contains": "taskId"
      }
    },
    {
      "name": "task.get with numeric taskId fails type check",
      "input": {
        "tool": "task.get",
        "taskId": 12345
      },
      "expect": {
        "status": "error",
        "error_contains": "Expecting a STRING"
      },
      "note": "taskId must be a string, not a number"
    },
    {
      "name": "task.get with valid taskId but no server",
      "input": {
        "tool": "task.get",
        "taskId": "test-task-1",
        "baseUrl": "http://localhost:19876"
      },
      "expect": {
        "status": "error",
        "error_contains": "curl"
      },
      "note": "Uses a non-standard port to ensure curl error regardless of whether Chorus is running"
    },
    {
      "name": "task.get with custom baseUrl",
      "input": {
        "tool": "task.get",
        "taskId": "test-task-2",
        "baseUrl": "http://localhost:9999"
      },
      "expect": {
        "status": "error",
        "error_contains": "curl"
      },
      "note": "Expected to fail with curl error since no server is running at port 9999"
    },
    {
      "name": "task.get ignores extra fields",
      "input": {
        "tool": "task.get",
        "taskId": "test-task-3",
        "baseUrl": "http://localhost:19876",
        "extra": "ignored"
      },
      "expect": {
        "status": "error",
        "error_contains": "curl"
      },
      "note": "Extra fields should be silently ignored; error is curl because no server running"
    }
  ]
}
