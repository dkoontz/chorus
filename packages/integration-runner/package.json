{
  "name": "chorus-integration-runner",
  "version": "0.1.0",
  "description": "Integration test runner for Chorus applications",
  "scripts": {
    "build": "gren make Main --output=build/runner-tmp && tail -n +2 build/runner-tmp > build/runner.js && bun build --compile build/runner.js --outfile build/runner && rm build/runner-tmp",
    "clean": "rm -rf build .gren"
  },
  "keywords": ["gren", "chorus", "testing"],
  "license": "MIT"
}
