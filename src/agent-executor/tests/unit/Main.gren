module Main exposing (main)

import SpecTests
import Test exposing (Test, describe)
import Test.Runner.Node exposing (Program, run)


main : Program
main =
    run
        (describe "Agent Executor Tests"
            [ SpecTests.suite
            ]
        )
